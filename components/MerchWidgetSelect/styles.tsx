import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        marginTop: 10,
        height: 330,
    },
    title: {
        color: "white",
        fontSize: 24,
        fontWeight: "bold",
        letterSpacing: -1,
    }
})

export default styles;