import { StyleSheet, Dimensions } from 'react-native'

const styles = StyleSheet.create({
    container : {
        marginTop: 10,
        color: "white",
    },
    title: {
        color: "white",
        fontSize: 24,
        fontWeight: "bold",
        letterSpacing: -1,
        marginTop: 10,
    }
})

export default styles